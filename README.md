# Image-captioning
Built an Image Caption Generator using the BLIP (Bootstrapping Language–Image Pre-training) model to automatically generate captions for images. Implemented using Python and the Hugging Face Transformers library with PyTorch for model inference. Developed an interactive interface with Gradio that supports both local image uploads and image URLs.
